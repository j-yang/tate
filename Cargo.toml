[package]
name = "tate"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "A self-contained structured diff library for Rust — line diff, grid alignment, tree comparison, and inline word-level highlighting"
repository = "https://github.com/j-yang/tate"
keywords = ["diff", "compare", "alignment", "lcs", "structured"]
categories = ["text-processing", "development-tools"]

[features]
default = ["serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
# roxmltree is NOT a tate dependency — format parsing happens in mumford.

[dev-dependencies]
serde_json = "1"