[package]
name = "tate"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Structured diff primitives: grid alignment, tree diff, and inline replacement pairing"
repository = "https://github.com/j-yang/tate"

[dependencies]
roxmltree = "0.20"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"